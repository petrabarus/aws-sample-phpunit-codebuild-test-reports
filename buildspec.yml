version: '0.2'
phases:
  install:
    runtime-versions:
      nodejs: '10'
      php: '7.3'
    commands:
    - npm install
    - composer install
  build:
    commands:
    - ./vendor/bin/phpunit
artifacts:
  files:
  - build/reports.xml